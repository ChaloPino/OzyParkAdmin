@if (Condition)
{
    @Then
}
else
{
    @Else
}

@code {
    [Parameter]
    public bool Condition { get; set; }

    [Parameter]
    public RenderFragment? Then { get; set; }

    [Parameter]
    public RenderFragment? Else { get; set; }
}
